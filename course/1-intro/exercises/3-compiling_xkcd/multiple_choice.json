{
  "question": "Go code generally runs faster than interpreted languages and compiles slower than other compiled languages like C and Rust",
  "answers": [
    "faster, faster",
    "faster, slower",
    "slower, faster",
    "slower, slower"
  ]
}
